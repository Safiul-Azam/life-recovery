import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const namazSlice = createSlice({
  name: "namaz",
  initialState,
  reducers: {
    
  },
});

export const {} = namazSlice.actions;
export default namazSlice.reducer;
