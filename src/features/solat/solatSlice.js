import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const solatSlice = createSlice({
  name: "solat",
  initialState,
  reducers: {},
});

export const {} = solatSlice.actions;
export default solatSlice.reducer;
